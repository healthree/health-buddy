<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Healthree</title>
  {{!-- Change to respective style sheet name --}}
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <div class="main-wrap">
    <header>
      <h1>
        Health Buddy
      </h1>
      <nav class="main-nav">
        <a href="/about-us">About Us/Our Team</a>
        <a href="/address">Address</a>
        {{#if loggedIn}}
        {{!-- Change to respective javascript name --}}
        {{!-- <h3>Welcome {{ user.name }}</h3> --}}
        <a href="/dashboard">Patient Information</a>
        <button id="logout" class="btn-no-style">Signout</button>
        {{else}}
        <a href="/login">Admin Access</a>
        {{/if}}
        <a href="/phone">Phone</a>
      </nav>
    </header>
    {{!-- make background for main a picture --}}
    <main class="main-main">
      {{#if loggedIn}}
      <button id="patient-btn">New Patient</button>
      <button id="edit-btn">Edit File</button>
      <button id="delete-btn">Delete File</button>
      <button id="access-btn">View File</button>
      <button id="doctors-btn">Doctors List</button>

      {{#if patients.length}}
      <section>
        <h2>Your Patients:</h2>
        <ol>
          {{#each patients as |patient|}}
          <li style="list-style-type: none;">
            {{> patient-information patient}}
            {{!--/maim/edit/{{patient.id}} --}}
            <a href="/edit/{{patient.id}}" id="edit-patient-btn">Edit</a>
            <button id="delete-patient-btn">Delete</button>
          </li>
          {{/each}}
        </ol>
      </section>
      {{/if}}
      
      {{else}}
      <button id="bmi-btn">Check BMI</button>
      <button id="who-btn">Who Should I See?</button>
      {{/if}}
    </main>
  </div>
  <footer>
    <a href="/contact-us">Contact Us</a>
  </footer>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>
</html>

<script src="/javascript/edit-patient.js"></script>
<script src="/javascript/delete-patient.js"></script>